test -f /usr/local/opt/asdf/asdf.sh && source /usr/local/opt/asdf/asdf.sh
test -f /usr/local/etc/bash_completion.d/asdf.bash && source /usr/local/etc/bash_completion.d/asdf.bash

type asdf &>/dev/null

if [ $? -eq 0 ]; then
  install_ruby_version(){
    asdf install ruby $1
  }
fi
